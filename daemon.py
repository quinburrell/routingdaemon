#TODO
#read config function
#open connection function
def hello(port):
    """constructs a hello packet and sends it to port"""
def readconfig(filename):
    """reads the configfile inputed by the user
    assigns itself a router ID, inpu and output ports and starts a timer on itself"""
def openports(ports):
    """opens UDP connections with ports"""